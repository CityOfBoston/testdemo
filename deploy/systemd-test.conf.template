[Unit]
Description={{ INSTANCE_NAME }}
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker start -a {{ APP_NAME }}-{{ INSTANCE_NAME }}
ExecStop=/usr/bin/docker stop -t 2 {{ APP_NAME }}-{{ INSTANCE_NAME }}

[Install]
WantedBy=default.target
